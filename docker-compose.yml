version: '3.0'
services:
  mongo:
    image: mongo:4.2
    container_name: "mongo"
    volumes:
      - mongo_data:/data/db
    restart: always

  princess_trixie:
    container_name: "Princess-Trixie"
    build: .
    depends_on:
      - mongo
    environment:
      - PRINCESS_TRIXIE_TOKEN=${PRINCESS_TRIXIE_TOKEN}
    restart: always

volumes:
  mongo_data: